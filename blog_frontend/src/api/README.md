Development note:
If your backend is not ready, you can quickly mock requests by running a lightweight JSON server or intercepting fetch.
This project ships only placeholders and expects a real REST API at REACT_APP_API_BASE_URL.
